# Q&A API (FastAPI + PostgreSQL)

Простой API-сервис вопросов и ответов.

## Требования
- Docker, docker-compose

## Запуск
1) Скопируйте `.env.example` в `.env` и при необходимости измените значения.
2) Запустите:
```bash
docker-compose up --build
# Q&A API (FastAPI + PostgreSQL)

Простой и эффективный API-сервис для управления вопросами и ответами, разработанный с использованием FastAPI и PostgreSQL. Этот проект демонстрирует современный подход к созданию веб-сервисов с акцентом на производительность, надежность и удобство развертывания с помощью Docker.

## 🌟 Описание проекта

Этот API позволяет пользователям создавать вопросы, а затем добавлять к ним ответы. Он идеально подходит для создания основы для форума, системы поддержки или любого другого приложения, где требуется функциональность вопросов и ответов.

Основные возможности:
*   Создание, получение, список и удаление вопросов.
*   Создание, получение и удаление ответов, связанных с конкретными вопросами.
*   Автоматическое управление связями между вопросами и ответами (например, удаление ответов при удалении вопроса).

## 🚀 Технологии

Проект построен на следующих ключевых технологиях:

*   **FastAPI**: Высокопроизводительный веб-фреймворк для создания API на Python 3.7+.
*   **PostgreSQL**: Мощная и надежная реляционная база данных.
*   **SQLAlchemy**: Мощный и гибкий ORM (Object-Relational Mapper) для взаимодействия с базой данных.
*   **Alembic**: Инструмент для миграции базы данных, интегрированный с SQLAlchemy.
*   **Pydantic**: Библиотека для валидации данных и управления настройками, используемая для схем API и конфигурации.
*   **Docker & Docker Compose**: Для легкого развертывания и управления сервисами (приложение и база данных).
*   **Uvicorn**: ASGI-сервер для запуска FastAPI приложения.
*   **Psycopg2-binary**: Адаптер PostgreSQL для Python.
*   **Pytest**: Фреймворк для тестирования.
*   **Httpx**: Клиент для выполнения HTTP-запросов в тестах.

## 📁 Структура проекта

Проект организован следующим образом:
. ├── alembic/ # Конфигурация и скрипты миграций Alembic ├── app/ # Основной код приложения FastAPI │ ├── api/ # API-эндпоинты │ │ └── endpoints/ # Модули с роутами для вопросов и ответов │ ├── core/ # Основные настройки и утилиты (конфигурация, логирование) │ ├── crud/ # Операции CRUD для взаимодействия с БД │ ├── db/ # Конфигурация базы данных (сессия, базовая модель) │ ├── models/ # Модели SQLAlchemy для таблиц БД │ ├── schemas/ # Схемы Pydantic для валидации данных API │ └── main.py # Главный файл приложения FastAPI ├── tests/ # Тесты для API ├── .env.example # Пример файла с переменными окружения ├── .gitignore # Файл для игнорирования Git ├── Dockerfile # Инструкции для сборки Docker-образа приложения ├── docker-compose.yml # Конфигурация Docker Compose для запуска сервисов ├── entrypoint.sh # Скрипт запуска приложения в Docker-контейнере ├── requirements.txt # Зависимости Python └── README.md # Этот файл


## ⚙️ Требования

*   Docker
*   Docker Compose

## 🚀 Запуск

Для запуска приложения выполните следующие шаги:

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/OlegLazukov/App_api.git # Замените на свой URL, если он отличается
    cd App_api # Перейдите в директорию проекта
    ```

2.  **Создайте файл `.env`:**
    Скопируйте `.env.example` в `.env` и при необходимости измените значения переменных окружения.
    ```bash
    cp .env.example .env
    ```
    Содержимое `.env` по умолчанию:
    ```
    POSTGRES_USER=postgres
    POSTGRES_PASSWORD=postgres
    POSTGRES_DB=qa_db
    DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/qa_db
    APP_ENV=dev
    ```

3.  **Запустите Docker Compose:**
    Эта команда соберет Docker-образы и запустит все сервисы (PostgreSQL и FastAPI приложение).
    ```bash
    docker-compose up --build
    ```
    Приложение будет доступно по адресу `http://localhost:8000`.

## 🌐 API Эндпоинты

Ниже приведены основные эндпоинты API:

### ❓ Вопросы (`/questions`)

| Метод    | Путь                       | Описание                                     | Схема запроса (Payload)           | Схема ответа (Response)           |
| :------- | :------------------------- | :------------------------------------------- | :-------------------------------- | :-------------------------------- |
| `GET`    | `/questions/`              | Получить список всех вопросов.               | -                                 | `list[QuestionOut]`               |
| `POST`   | `/questions/`              | Создать новый вопрос.                        | `{"text": "Текст вопроса"}`       | `QuestionOut`                     |
| `GET`    | `/questions/{question_id}` | Получить вопрос по ID с его ответами.        | -                                 | `QuestionWithAnswers`             |
| `DELETE` | `/questions/{question_id}` | Удалить вопрос по ID (удаляет и все ответы). | -                                 | `204 No Content`                  |

### 💬 Ответы (`/answers`)

| Метод    | Путь                               | Описание                                     | Схема запроса (Payload)                               | Схема ответа (Response) |
| :------- | :--------------------------------- | :------------------------------------------- | :---------------------------------------------------- | :---------------------- |
| `POST`   | `/answers/questions/{question_id}/answers/` | Создать ответ для конкретного вопроса.       | `{"user_id": "ID пользователя", "text": "Текст ответа"}` | `AnswerOut`             |
| `GET`    | `/answers/{answer_id}`             | Получить ответ по ID.                        | -                                                     | `AnswerOut`             |
| `DELETE` | `/answers/{answer_id}`             | Удалить ответ по ID.                         | -                                                     | `204 No Content`        |

## 🧪 Тестирование

Проект включает набор тестов, которые можно запустить с помощью `pytest`.
Тесты находятся в директории `tests/`.

Для запуска тестов внутри контейнера `app` (после `docker-compose up`):
```bash
docker-compose exec app pytest
Или, если вы хотите запустить их локально (убедившись, что у вас установлены зависимости из requirements.txt и настроена тестовая БД):

pytest
